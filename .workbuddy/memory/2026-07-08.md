# 2026-07-08

## 邮箱模块审计（对照 enough_mail 2.1.7）
- 产出报告：`EMAIL_AUDIT_REPORT_V3.md`（对照官方能力清单 + 逐文件源码核查）。
- 关键 P0 问题（已逐行验证源码）：
  - compose_page.dart:257-263 回复未传 `replyAll:false`，默认 true → "回复"实为"回复全部"。
  - email_sync_service.dart:436-442 `_searchSinceUid` 忽略 lastUid，改用日期搜索；增量同步不跳过已同步 UID → 每次重复拉近7天。
  - mail_data_source.dart:62 `downloadSizeLimit:100KB` + 未用 `fetchMessagePart` → 大邮件附件无法保存。
  - email_list_page.dart:293-294 窄屏 `flagged` 直接 `return false` → 星标永远为空。
  - email_detail_view.dart:401-416 删除先本地后服务端，catch(_) 吞错 → 服务端失败时本地丢失；msg==null 时服务端不清理。
  - email_detail_view.dart:129-138 仅当 mimeMessage!=null 才标服务端已读 → 仅信封行服务端残留未读。
- P1：testConnection 硬编码 `supportsIdle:false`（:603）致 IDLE 永不启用；email_toolbar:98 联系人按钮空实现；attachment_list_widget:345 分享 TODO；多处 catch(_) 静默；compose build 内改状态；emailRepositoryProvider 可空 no-op；窄屏无文件夹切换；flutter_local_notifications/workmanager 声明未导入（仅 Windows 托盘）。
- 待实现（enough_mail 已支持但项目未用/无UI）：搜索UI、移动到文件夹、文件夹增删、垃圾标记、会话视图、POP3、mailto、DKIM、QUOTA、IDLE。
- 注：V2 报告的 email→task、FTS5 等项经复核已修复，本报告未重复计入。
