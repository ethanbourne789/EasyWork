# 2026-08-14

## Windows 绿色版构建尝试（build-green.ps1）
- 验证配置均就绪：bundle.active:false/targets:[]、+crt-static、custom-protocol feature、pnpm-workspace.yaml allowBuilds esbuild、CSP connect-src 放开、MSVC link.exe 在 D:\VS Community。
- 修复前端类型门：`src/features/tasks/taskApi.ts` 删除未使用导入 `TaskTag`（TS6196）。
- **关键改进**：本沙箱直连 crates.io 极慢，给 `~/.cargo/config.toml` 加了 rsproxy 国内镜像（`[source.crates-io] replace-with="rsproxy-sparse"`）。从零冷构建前必须配，否则下载阶段几十分钟。已实测 rsproxy sparse index 0.1s vs crates.io 0.88s。
- 构建推进到 Rust 编译阶段后**被阻塞**：用户进行中的 `sync` 功能模块有 10 个 Rust 编译错误（工作树里 settings/ 几个文件导入路径当时错误、已被用户即时修正；另有 sync 模块自身的重构未完成错误：
  - 机械性（可直修）：config.rs:42 `to_rfc3333_opts`→`to_rfc3339_opts`；engine.rs:179 `&dyn (ToSql+Sync)` 需括号；engine.rs:176 if/else 分支 `Box<Option<String>>` 不一致。
  - 需用户意图：postgres.rs:13 `rustls_platform_verifier::builder()` 在锁定的 0.5.3 不存在；commands.rs:126 `SyncResult` 用旧字段 fetched/inserted/folders 但 `sync::SyncResult` 已是 success/records_uploaded/records_downloaded/error（重构没改全）。
- 已向用户报告并询问处理方式（我修 / 用户回 Trae 修 / 先排除 sync 出包）。待用户决定。

## 全面代码审阅（08-14 晚，详见 docs/code-review-2026-08-14.md）
- 架构现状：邮件已迁 Tauri 本地 SQLite（keyring 存密码、rustls 平台 CA）；任务/笔记/记账/日历仍直连 Supabase；08-14 新提交的「local-first PostgreSQL 同步」是半成品——sync 引擎同步的本地业务表无数据来源（前端不写本地），唯一同步的是 email_accounts。
- 🔴 sync 引擎 Rust 确定性 bug（cargo check 能过但运行必错）：① LWW 用未定义表别名 `t.` 且 `let _ = pg.execute()` 吞错 → 云端更新永远静默失败；② `row.get::<_,Option<String>>` 把 INTEGER 列读成 NULL → amount_cents/sort_order 等 NOT NULL 列整行上传失败；③ task_tags/note_tags 复合主键表 `ON CONFLICT ("id")` 无 id 列必失败；④ download UPDATE 参数索引写死 `?{columns.len()+1}` 越界；⑤ 错误全部静默 + count 照加。
- taskApi/notesApi/financeApi/calendarApi 为零引用死代码，调用的 40 个 Rust 命令（task_list_all 等）不存在。
- 前端 bug：notify.ts fireBudgetWarnings 不按 year_month 过滤 + overall 永不触发 + 忽略 carry_over；realtime 超 5 次重试后永久 unavailable；NoteEditor 切笔记后标签竞态覆盖；useTasks 周期任务 TOCTOU 重复生成；authStore logout 吞错。
- 测试：3 文件失败（authStore/notify 缺 Supabase env 未 mock；TransactionForm 1 用例断言漂移）。
- 08-11 高危项大多已修复（withGlobalTauri/keyring/TLS/占位死链/0031 drop mail tables）。

## P0 bug 修复（08-14 晚，已完成）
- 修复 sync 引擎 Rust（engine.rs 重写，cargo check 通过）：
  - C2 LWW 用目标表名替代未定义别名 `t.`；C3 按 ValueRef 类型化读取（Integer/Real 不再变 NULL）；C4 新增 table_pk() 处理 task_tags/note_tags 复合主键；C5 下载 UPDATE 参数索引递增；C6 错误不再静默（写 sync_log + SyncResult.error，失败不更新 last_sync_at）；下载条件 `!=` → `IS DISTINCT FROM`（修复云端 NULL 导致跨设备拉不到数据）；async 中 blocking_lock → lock().await。
- 测试基建：vitest.config.ts 加 test.env（Supabase 假 env）；test-setup.ts import i18n → 10 文件 60 用例全过。
- fireBudgetWarnings：按 year_month 过滤 + overall 用总支出 + 计入 carry_over；notifications.ts 同步计入 carry_over。
- 遗留未修（架构决策）：C1 数据源分裂——前端业务仍直连 Supabase，sync 引擎同步空表；taskApi 等 4 个死代码 api + 40 个未实现的 Rust 命令待用户决定方向。

## 二轮 bug 修复（08-14 晚，已完成，验证全绿：tsc/eslint 0 问题、vitest 61 用例、cargo check）
- realtime（H3/H9）：超 MAX_RETRIES 后每 60s 低频探测重连（不再永久失效）；safeTauriListen then 回调检查 isCleanedUp（修泄漏）。
- NoteEditor（H5）：tagIdsReadyRef 守卫，切换笔记后标签未就绪时忽略点击。
- authStore（M5）：logout 改 async，signOut 成功才清空会话，失败保留+throw；Settings 登出 await+toast。
- useTasks（M6/H4）：创建时 task_tags 失败降级日志不再抛错（避免重复任务）；完成周期任务用原子抢占（update recurrence_next=null WHERE =nextDue）防并发重复生成，标签复制移到变更后。
- db.rs（M1）：重构为版本化迁移——仅全新库 DROP+CREATE，旧库 ALTER-only（原实现会对非最新版本库 DROP 全部业务表，数据丢失隐患已修）。
- Settings 导出 try/catch、BudgetList carry_over 文案、SyncConfigForm/GlobalSearch eslint 依赖（现在 0 警告）。
- 邮件草稿（H8）：错误文案改「功能开发中」（后端 mail_save_draft 命令待 P1）。

## local-first 架构迁移完成（08-14 深夜，验证全绿）
- 任务/笔记/记账/日历迁入本地 SQLite：新建 src-tauri/src/business.rs（~1800 行）实现 74 个命令（task 15/note 17/finance 16/calendar 11 + 备份/收据），lib.rs 注册，前端 74 invoke 100% 对齐。
- 关键设计：输出结构=前端类型（金额元/bool/JSON 对象/year_month number/carry_over 分转元）；`null_fields` 参数解决 Tauri IPC「未传 vs 显式 null」不可区分；data_export_all/import_all/clear_all 备份；receipt_save/open 收据本地化；calendar_sync_subscription 复用 calendar_sync.rs。
- db.rs v4 迁移：budgets.carry_over_cents、notes.content_text/cover_url、note_tag_master/note_note_tags 新表；schema.rs 云端同步 + engine.rs 同步表清单同步更新。
- 前端 4 个 hooks（useTasks/useNotes/useFinance/useCalendar）从 Supabase 切到 api 层；fireBudgetWarnings 数据源本地化；Settings 导出/导入/清空本地化；TransactionForm 收据本地化。
- ⚠️ 教训：python re.sub 用非贪婪 `.*?` 跨行删除时，若目标无匹配的终止符（如 `];` 而实际是 `] as const;`），会匹配到文件后部导致大面积误删——已从 git 恢复 Settings.tsx 重建（用户 SyncSettings 改动 + 本地化改动重新应用）。
- 遗留：认证仍走 Supabase（profiles/authStore）；realtime 订阅保留（无本地数据源事件）；邮件模块不动。

## 认证本地化完成（08-14 深夜，验证全绿）
- 认证不再走 Supabase：db.rs v5 迁移 users 表；business.rs 新增 auth_register/auth_login/auth_get_user/auth_update_profile/auth_change_password 5 命令（argon2 0.5 哈希，features std+rand）。
- 前端：authApi.ts 新建；authStore 重写（session→user，localStorage easywork:user_id 持久化，login/register/logout/refreshUser）；useAuth 改本地会话恢复；Login/Register 去 Supabase+演示入口（注册即自动登录）；ChangePasswordDialog/useProfile/Settings 头像全本地（头像存 base64 data URL）。
- 路由守卫 session→user 适配（App/router/Sidebar/Dashboard）。
- 残留 Supabase：realtime 订阅、mail/migrateFromSupabase、supabase.ts（仅这两处用）。
- 测试：authStore/useAuth 测试重写适配本地；10 文件 65 用例全过；tsc/eslint/cargo check 全绿。
