# 2026-08-14

## Windows 绿色版构建尝试（build-green.ps1）
- 验证配置均就绪：bundle.active:false/targets:[]、+crt-static、custom-protocol feature、pnpm-workspace.yaml allowBuilds esbuild、CSP connect-src 放开、MSVC link.exe 在 D:\VS Community。
- 修复前端类型门：`src/features/tasks/taskApi.ts` 删除未使用导入 `TaskTag`（TS6196）。
- **关键改进**：本沙箱直连 crates.io 极慢，给 `~/.cargo/config.toml` 加了 rsproxy 国内镜像（`[source.crates-io] replace-with="rsproxy-sparse"`）。从零冷构建前必须配，否则下载阶段几十分钟。已实测 rsproxy sparse index 0.1s vs crates.io 0.88s。
- 构建推进到 Rust 编译阶段后**被阻塞**：用户进行中的 `sync` 功能模块有 10 个 Rust 编译错误（工作树里 settings/ 几个文件导入路径当时错误、已被用户即时修正；另有 sync 模块自身的重构未完成错误：
  - 机械性（可直修）：config.rs:42 `to_rfc3333_opts`→`to_rfc3339_opts`；engine.rs:179 `&dyn (ToSql+Sync)` 需括号；engine.rs:176 if/else 分支 `Box<Option<String>>` 不一致。
  - 需用户意图：postgres.rs:13 `rustls_platform_verifier::builder()` 在锁定的 0.5.3 不存在；commands.rs:126 `SyncResult` 用旧字段 fetched/inserted/folders 但 `sync::SyncResult` 已是 success/records_uploaded/records_downloaded/error（重构没改全）。
- 已向用户报告并询问处理方式（我修 / 用户回 Trae 修 / 先排除 sync 出包）。待用户决定。

## 全面代码审阅（08-14 晚，详见 docs/code-review-2026-08-14.md）
- 架构现状：邮件已迁 Tauri 本地 SQLite（keyring 存密码、rustls 平台 CA）；任务/笔记/记账/日历仍直连 Supabase；08-14 新提交的「local-first PostgreSQL 同步」是半成品——sync 引擎同步的本地业务表无数据来源（前端不写本地），唯一同步的是 email_accounts。
- 🔴 sync 引擎 Rust 确定性 bug（cargo check 能过但运行必错）：① LWW 用未定义表别名 `t.` 且 `let _ = pg.execute()` 吞错 → 云端更新永远静默失败；② `row.get::<_,Option<String>>` 把 INTEGER 列读成 NULL → amount_cents/sort_order 等 NOT NULL 列整行上传失败；③ task_tags/note_tags 复合主键表 `ON CONFLICT ("id")` 无 id 列必失败；④ download UPDATE 参数索引写死 `?{columns.len()+1}` 越界；⑤ 错误全部静默 + count 照加。
- taskApi/notesApi/financeApi/calendarApi 为零引用死代码，调用的 40 个 Rust 命令（task_list_all 等）不存在。
- 前端 bug：notify.ts fireBudgetWarnings 不按 year_month 过滤 + overall 永不触发 + 忽略 carry_over；realtime 超 5 次重试后永久 unavailable；NoteEditor 切笔记后标签竞态覆盖；useTasks 周期任务 TOCTOU 重复生成；authStore logout 吞错。
- 测试：3 文件失败（authStore/notify 缺 Supabase env 未 mock；TransactionForm 1 用例断言漂移）。
- 08-11 高危项大多已修复（withGlobalTauri/keyring/TLS/占位死链/0031 drop mail tables）。

## P0 bug 修复（08-14 晚，已完成）
- 修复 sync 引擎 Rust（engine.rs 重写，cargo check 通过）：
  - C2 LWW 用目标表名替代未定义别名 `t.`；C3 按 ValueRef 类型化读取（Integer/Real 不再变 NULL）；C4 新增 table_pk() 处理 task_tags/note_tags 复合主键；C5 下载 UPDATE 参数索引递增；C6 错误不再静默（写 sync_log + SyncResult.error，失败不更新 last_sync_at）；下载条件 `!=` → `IS DISTINCT FROM`（修复云端 NULL 导致跨设备拉不到数据）；async 中 blocking_lock → lock().await。
- 测试基建：vitest.config.ts 加 test.env（Supabase 假 env）；test-setup.ts import i18n → 10 文件 60 用例全过。
- fireBudgetWarnings：按 year_month 过滤 + overall 用总支出 + 计入 carry_over；notifications.ts 同步计入 carry_over。
- 遗留未修（架构决策）：C1 数据源分裂——前端业务仍直连 Supabase，sync 引擎同步空表；taskApi 等 4 个死代码 api + 40 个未实现的 Rust 命令待用户决定方向。

## 二轮 bug 修复（08-14 晚，已完成，验证全绿：tsc/eslint 0 问题、vitest 61 用例、cargo check）
- realtime（H3/H9）：超 MAX_RETRIES 后每 60s 低频探测重连（不再永久失效）；safeTauriListen then 回调检查 isCleanedUp（修泄漏）。
- NoteEditor（H5）：tagIdsReadyRef 守卫，切换笔记后标签未就绪时忽略点击。
- authStore（M5）：logout 改 async，signOut 成功才清空会话，失败保留+throw；Settings 登出 await+toast。
- useTasks（M6/H4）：创建时 task_tags 失败降级日志不再抛错（避免重复任务）；完成周期任务用原子抢占（update recurrence_next=null WHERE =nextDue）防并发重复生成，标签复制移到变更后。
- db.rs（M1）：重构为版本化迁移——仅全新库 DROP+CREATE，旧库 ALTER-only（原实现会对非最新版本库 DROP 全部业务表，数据丢失隐患已修）。
- Settings 导出 try/catch、BudgetList carry_over 文案、SyncConfigForm/GlobalSearch eslint 依赖（现在 0 警告）。
- 邮件草稿（H8）：错误文案改「功能开发中」（后端 mail_save_draft 命令待 P1）。

## local-first 架构迁移完成（08-14 深夜，验证全绿）
- 任务/笔记/记账/日历迁入本地 SQLite：新建 src-tauri/src/business.rs（~1800 行）实现 74 个命令（task 15/note 17/finance 16/calendar 11 + 备份/收据），lib.rs 注册，前端 74 invoke 100% 对齐。
- 关键设计：输出结构=前端类型（金额元/bool/JSON 对象/year_month number/carry_over 分转元）；`null_fields` 参数解决 Tauri IPC「未传 vs 显式 null」不可区分；data_export_all/import_all/clear_all 备份；receipt_save/open 收据本地化；calendar_sync_subscription 复用 calendar_sync.rs。
- db.rs v4 迁移：budgets.carry_over_cents、notes.content_text/cover_url、note_tag_master/note_note_tags 新表；schema.rs 云端同步 + engine.rs 同步表清单同步更新。
- 前端 4 个 hooks（useTasks/useNotes/useFinance/useCalendar）从 Supabase 切到 api 层；fireBudgetWarnings 数据源本地化；Settings 导出/导入/清空本地化；TransactionForm 收据本地化。
- ⚠️ 教训：python re.sub 用非贪婪 `.*?` 跨行删除时，若目标无匹配的终止符（如 `];` 而实际是 `] as const;`），会匹配到文件后部导致大面积误删——已从 git 恢复 Settings.tsx 重建（用户 SyncSettings 改动 + 本地化改动重新应用）。
- 遗留：认证仍走 Supabase（profiles/authStore）；realtime 订阅保留（无本地数据源事件）；邮件模块不动。

## 认证本地化完成（08-14 深夜，验证全绿）
- 认证不再走 Supabase：db.rs v5 迁移 users 表；business.rs 新增 auth_register/auth_login/auth_get_user/auth_update_profile/auth_change_password 5 命令（argon2 0.5 哈希，features std+rand）。
- 前端：authApi.ts 新建；authStore 重写（session→user，localStorage easywork:user_id 持久化，login/register/logout/refreshUser）；useAuth 改本地会话恢复；Login/Register 去 Supabase+演示入口（注册即自动登录）；ChangePasswordDialog/useProfile/Settings 头像全本地（头像存 base64 data URL）。
- 路由守卫 session→user 适配（App/router/Sidebar/Dashboard）。
- 残留 Supabase：realtime 订阅、mail/migrateFromSupabase、supabase.ts（仅这两处用）。
- 测试：authStore/useAuth 测试重写适配本地；10 文件 65 用例全过；tsc/eslint/cargo check 全绿。

## Windows 绿色版构建成功（08-14 晚，npm run build:green）
- 产物：release-green/EasyWork.exe（24 MB，比此前 9MB 大——新增 argon2/rustls 等依赖 + 前端 dist 膨胀，release+strip 后 24MB 合理）。
- 验证：导入表无 VCRUNTIME/MSVCP/vcruntime/msvcp（+crt-static 静态链接 OK），仅系统 DLL（kernel32/user32/advapi32/ws2_32 等）；目标机需 WebView2 Runtime（Win10/11 自带）。
- 本次 cargo release 增量 1m42s（argon2 新依赖冷编译），前端 vite build 7.9s。
- 注意：vite 警告 tauri.ts 被动态+静态混合导入（无功能影响，仅 chunk 划分提示）。

## 提交项目到 Gitee（08-14 深夜）
- 安装 gitee CLI（@gitee/gitee-cli v0.2.3，npm 全局）并认证账号 `ethanbourne789`（昵称「遁去的壹」）。
- 将全部未提交改动（local-first 架构迁移 + 认证本地化，共 54 文件）一次性 commit 并 push 到 `origin/main`（https://gitee.com/ethanbourne789/EasyWork.git）。
- 提交用仓库级 git user（name=ethanbourne789，email=...@users.noreply.gitee.com）；push 凭证用 gitee token 内联 URL（未写入 .git/config，不触发 credential helper 弹窗）。
- ⚠️ 该 gitee token 已在聊天中暴露，建议稍后在 Gitee 令牌页吊销并轮换。

## 🔴 绿色版白屏根因与修复（08-14 晚，重要教训）
- 现象：绿色版启动后只有底色（JS 从未挂载）。
- 根因：`.env` 缺失 → `src/lib/supabase.ts` 顶层 `if (!env) throw`（该 throw 编译进 bundle）→ **bundle 加载求值即崩溃**，ReactDOM.createRoot 未执行，错误边界也来不及显示 → 纯底色。
- vite build 不报错（build 期不执行模块顶层代码，只是打包）。
- 修复：supabase.ts 不再 throw，env 缺失用占位地址创建客户端（优雅降级）；新增 `IS_SUPABASE_CONFIGURED` 导出；`useRealtimeSync` 在未配置时直接 return（避免对占位域名无效连接/重试）。
- 教训：**顶层 throw 的模块（依赖 env/全局状态）是白屏高发点**；绿色版（无 .env）构建后应检查 bundle 是否残留这类错误串（`grep -r "缺少 Supabase" dist/assets/*.js`）。
- 已重新构建 release-green/EasyWork.exe（23.29MB，assets 内嵌确认、无 throw 串）。

## 绿色版运行错误排查（08-14 晚）
- **错误 2（确定 bug，已修）**：Dashboard 加载 budgets 报错 `no such column: updated_at in SELECT ... FROM budgets` —— db.rs 的 budgets 表没有 updated_at 列（v3 迁移加的是 sync 列，未含 updated_at），但 Rust business.rs `budget_list_all/update` SQL 引用了。**修复**：db.rs 新增 v6 迁移 `ALTER TABLE budgets ADD COLUMN updated_at`（DEFAULT strftime，兼容历史库）+ create_all_tables 同步加入 + schema.rs 云端 budgets 同步加。重启 exe 自动跑 v6 迁移修复旧库。
- **错误 1（`invalid args \`userId\``）**：前端 grep 无任何 `userId:` 作为对象 key 的代码（bundle 也仅 supabase vendor chunk 有，无 invoke 引用）。暂未定位到根因；加固 authApi.getUser 防御性空值检查防边缘触发。建议重测观察是否复现——若仍出现，用 `npm run tauri dev` + WebView2 devtools 定位 console 错误。

## local-first 全量审阅（08-14 深夜，详见 docs/local-first-audit-2026-08-14.md）
- 结论：架构已彻底 local-first（业务 CRUD + 认证全本地），Supabase 已降级为可选能力；业务代码中 supabase.from/auth/storage 调用全部消失，仅剩 supabase.channel（realtime）。
- 同步层已提前就位：Rust sync/（tokio-postgres 直连任意 PG + LWW 冲突解决 + 云端自动建表）+ 前端 SyncConfigForm 已支持 supabase/aiven/render/custom 四 provider——与"后期选平台"规划吻合。
- 残留分级：P0 = ① tauri.conf.json CSP 硬编码 nbcsywzqvvputqitmpla.supabase.co 域名（应改 connect-src 'self' https: wss:）② supabase/ 目录整体是旧 schema 部署单元（migrations 与 sync/schema.rs 脱节，建议归档 docs/archive/）③ src/types/database.types.ts 旧 Supabase 生成类型（误导，建议删，supabase.ts 去泛型）。
- P1 = 邮件 migrateFromSupabase 一次性迁移工具、TransactionForm 测试的 supabase storage 死 mock、authErrors 注释、TaskBoardView 关键词含 "supabase"、env 变量名可中性化。
- P2 = commands.rs 默认 provider "supabase" → "custom"。

## P0/P1/P2 清理已执行（08-14 深夜，验证全绿：tsc/vitest 65 用例/cargo check）
- P0 ① tauri.conf.json CSP → `connect-src 'self' https: wss:`（去硬编码域名）② supabase/ 目录 → `docs/archive/supabase-legacy/`（+ARCHIVE.md，git 会识别 rename）③ 删 `src/types/database.types.ts`，supabase.ts 去泛型。
- P1 ④ migrateFromSupabase.ts 加"历史抢救工具"注释 ⑤ TransactionForm 测试删 supabase storage 死 mock（6 用例仍过）⑥ authErrors 注释通用化 ⑦ TaskBoardView 关键词 supabase→数据库/同步/迁移 ⑧ env 改名**暂缓**（取决 realtime 去留）。
- P2 ⑨ commands.rs 默认 provider→custom ⑩ SyncConfigForm 表单默认→custom ⑪ sync/config.rs 表默认值→custom。顺带修 seedDemoData.ts TS6133（nTravel 未用）。
- 🔴 **realtime 分析（供用户决策）**：lib.rs 已有 60s 自动云同步（sync_upload+download）；但同步下载完成后 **Rust 不 emit 事件**（唯一事件是 mail://sync-progress）。realtime 订阅（supabase.channel→postgres_changes）在云变更时 invalidate 前端，**但此刻本地库尚未下载（60s 周期）→ 刷新拿旧数据，与数据落地时序脱节**；且 postgres_changes 是 Supabase 平台专有（Aiven/Render 无）。结论：realtime 实际价值≈0，**建议删除**，替代方案 = Rust 同步完成 emit 通用事件（邮件模块先例）+ 前端监听 invalidate，平台无关且时序正确。

## 演示模式「以演示账号进入」功能（08-14 深夜）
- 需求：登录页加「以演示账号进入」按钮，一键进入并填入覆盖全模块的演示数据；涉及日期的数据永远近 1 个月、每次打开都是最新。
- 实现（已编译验证：cargo build 通过、tsc -b 通过）：
  - 后端 `src-tauri/src/business.rs` 新增 `demo_enter` 命令（已注册 lib.rs）：确保 `demo@easywork.app`（argon2 哈希 `demo123456`）存在并返回 `AuthUserOut`。
  - 前端 `src/lib/systemApi.ts`：`enterDemoSession()`(invoke demo_enter) + `clearAllData()`(invoke data_clear_all)。
  - 前端 `src/features/auth/seedDemoData.ts`：先 `data_clear_all` 再生成全模块示例数据（任务含标签/子任务/周期/逾期与即将到期；笔记含多级文件夹/笔记标签/置顶；记账含多账户/多级分类/收入支出转账/当月预算；日历含过去与未来事件/提醒/禁用订阅）。**所有日期相对 `now()` 计算**（交易近 28 天、日历过去+未来约 ±12 天），实现「每次打开最新」。
  - `authStore.enterDemo()`：调 enterDemoSession + seedDemoData + setUser + 置 `isDemo`，演示标记写 localStorage `easywork:demo_mode`；`clearSession`/`logout` 清标记与 isDemo。
  - `useAuth` 启动检测演示标记→调 `enterDemo()` 重新播种（每次打开刷新）；`Login.tsx` 加 Sparkles 按钮+分隔线+hint；`AppLayout` 顶部「演示模式」胶囊（isDemo 时）。
  - i18n（zh-CN/en-US）补 `demoMode`/`demoDivider`/`demoHint`/`demoEntering`/`demoEnterSuccess`（`enterDemo` 标签此前已存在）。
- 说明：演示播种会清空当前本地全部业务数据（local-first 无用户隔离，预期行为）；仅演示会话触发，真实账号不受影响。完整 GUI 运行需 `npm run build:green` 产 exe（本沙箱未跑 release 构建）。

## realtime 已删除（08-14 深夜，用户确认，验证全绿：tsc/vitest 65 用例/eslint/cargo check/pnpm install -8 包）
- 移除：`src/lib/supabase.ts`、`src/features/realtime/`、`mail/migrateFromSupabase.ts`、`App.tsx` useRealtimeSync 调用、`AppLayout.tsx` 实时状态横幅（isDemo 胶囊保留）、`Mail.tsx` 迁移调用+queryClient、语言文件 layout.realtime* 键、设置页「关于/数据存储/邮箱账号」旧 Supabase 文案改 local-first（zh/en）。
- package.json 移除 @supabase/supabase-js；CSP 收紧 `connect-src 'self'`（已确认前端无 fetch/WebSocket/axios）；`.env.example`/`vite-env.d.ts` 清空（VITE_DEMO_* 业务无引用，删除安全）。
- 遗留引用仅剩注释性历史说明（useAuth/useCalendar/useEmailTemplates/authErrors/Settings 注释）+ SyncConfigForm providerSupabase 标签（有效 provider）。
- 🔴 **关键架构事实**：lib.rs 每 60s 自动云同步（upload+download）已存在；但下载落地后 Rust 不 emit 事件——将来做跨设备实时刷新，应加 `cloud://sync-complete` 类事件（邮件 mail://sync-progress 为先例）。
